# Unity WebGL — nginx snippet
# Put inside the server {} block that serves /builds/OMPharma/
# Then: nginx -t && systemctl reload nginx

location ~* \.wasm$ {
    default_type application/wasm;
    add_header Content-Type application/wasm;
}

location ~* \.data$ {
    default_type application/octet-stream;
}

location ~* \.js$ {
    default_type application/javascript;
}

# Uncomment if build uses Gzip
# location ~* \.(data|wasm|js|framework\.js)\.gz$ {
#     gzip off;
#     add_header Content-Encoding gzip;
#     default_type application/octet-stream;
# }
# location ~* \.wasm\.gz$ {
#     gzip off;
#     add_header Content-Encoding gzip;
#     default_type application/wasm;
# }

# Uncomment if build uses Brotli
# location ~* \.wasm\.br$ {
#     gzip off;
#     add_header Content-Encoding br;
#     default_type application/wasm;
# }
